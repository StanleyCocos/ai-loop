[project]
name = "core"
version = "0.1.0"
description = "Core orchestration layer for ai-loop"
readme = "README.md"
authors = [
    { name = "StanleyCocos", email = "stanleycocoa@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
core = "core.main:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
